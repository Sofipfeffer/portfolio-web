import Layout from "../../components/Layout/Layout.jsx";

const NotFound = () => {
    return (
        <Layout>
            <h2>no se encuentra</h2>
        </Layout>
    );
};

export default NotFound;